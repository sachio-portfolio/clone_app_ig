<div class="col-xs-12 col-md-2">
</div>
<div class="col-xs-12 col-sm-8 col-md-8">
  <h1 class="mt-3 mb-4">FAVORITE LIST <i class="far fa-heart"></i></h1>
  <% @favorites.each do |favorite| %>
    <%= link_to image_tag(favorite.picture.image.thumb.url), picture_path(favorite.picture.id) %>
  <% end %>
  <br>
  <%= link_to 'BACK', pictures_path %>
</div>
<div class="col-xs-12 col-md-2">
</div>
